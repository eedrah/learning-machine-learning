Function notes

The difference between 'expmax' and 'expmaxOrig' is that expmax uses k-means to initialise the cluster assignments, and so is much faster than expmaxOrig - which just starts with a random initialisation of the clusters.